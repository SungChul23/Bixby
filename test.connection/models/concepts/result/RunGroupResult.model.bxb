structure (RunGroupResult) {
  description ("그룹 실행 결과 메시지만 포함하는 구조체")
  property (messages) {
    type (messages) // ✅ 음성 출력용
    description ("그룹 실행 결과 메시지")
    min (Required)
    max (Many)
  }
  property (success) {
    type (success) // ✅ 음성 출력용
    description ("그룹 실행 결과 메시지")
    min (Required)
    max (One)
  }
  property (groupName) {
    type (groupName) // ✅ 음성 출력용
    description ("그룹 실행 결과 메시지")
    min (Required)
    max (One)
  }
}