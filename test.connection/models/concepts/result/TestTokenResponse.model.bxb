structure (TestTokenResponse) {
  property (accessToken) {
    type (accessToken)
    min (Optional)
    max (One)
  }

  property (expires_in) {
    type (expires_in)
    min (Optional)
    max (One)
  }
}
