structure (userSession) {
  description ("로그인 후 사용자 세션 정보")
  property (nickname) {
    type (nickname)       // 사용자의 닉네임
    min (Optional)
  }
  property (accessToken) {
    type (accessToken)       // 액세스 토큰 문자열
    visibility (Private)    // 토큰은 Private으로 설정
    min (Required)
  }
}
