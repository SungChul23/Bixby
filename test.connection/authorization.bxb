authorization {
  user {
    oauth2-authorization-code ("깜빡") {
      authorize-endpoint (https://kauth.kakao.com/oauth/authorize) //인가 코드

      client-id (7fd6112fa7ebf21d960c2f2a64f6d690) //카카오톡 왈 REST API를 호출할 때 사용합니다.

      client-secret-key (__KEY_TO_ACCESS_CLIENT_SECRET__)

      scope (profile_nickname,profile_image,account_email)

      token-endpoint (https://kauth.kakao.com/oauth/token) //토큰 받기
    }
  }
}
